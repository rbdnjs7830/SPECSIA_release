# DraViE core
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0
opencv-python>=4.8
pyyaml>=6.0
tqdm>=4.60
six>=1.16           # used in Trainer logging
numpy>=1.24

# Evaluation metrics
lpips>=0.1.4
open-clip-torch>=2.20
torchmetrics[image]>=1.0
scikit-image>=0.21
scikit-learn>=1.3
pandas>=2.0

# Dataset construction (optional – needed only if rebuilding SPECSIA-15K)
# trimesh>=3.9        # installed into Blender's Python via setup.sh
